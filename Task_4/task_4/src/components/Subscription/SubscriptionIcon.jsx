import sendIcon from '../../assets/icons/airplane.png';

const SubscriptionIcon = () => {
  return <img src={sendIcon} alt="Send Icon" className="subscription-card__icon" />;
};

export default SubscriptionIcon;
